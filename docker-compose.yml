services:
  journal-bot:
    build: .
    container_name: journal-bot
    restart: on-failure:3
    env_file:
      - .env
    environment:
      - TZ=Asia/Shanghai
    volumes:
      - ./auth.json:/app/auth.json
